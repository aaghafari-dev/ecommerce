version: 2

models:
  - name: stg_order_product
    columns:
      - name: order_product_id
        tests: [unique, not_null]
      - name: order_id
        tests: [not_null]
      - name: product_id
        tests: [not_null]
