name: 'ecommerce'
version: '1.0.0'

# Tell dbt which profile to use
profile: 'ecommerce'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  ecommerce:
    01-staging:
      +database: PREP
      +schema: OLTP
      +materialized: table
    02-prep:
      +database: PREP
      +schema: STAR_SCHEMA
      +materialized: table